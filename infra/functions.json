[
  {
    "name": "cloud-penny-getClientMe",
    "description": "Get client profile",
    "aws_name_override": "cloud-penny-getClientMe",
    "handler": "index.handler",
    "runtime": "nodejs20.x",
    "memory": 128,
    "timeout": 10,
    "environment": { "TENANTS_TABLE": "cloudpenny-tenants" },
    "routes": [
      { "method": "GET", "path": "/api/v1/clients/me" }
    ]
  },
  {
    "name": "cloud-penny-deleteAccount",
    "description": "Update / delete client account",
    "aws_name_override": "cloud-penny-deleteAccount",
    "handler": "index.handler",
    "runtime": "nodejs20.x",
    "memory": 128,
    "timeout": 10,
    "environment": { 
      "TENANTS_TABLE": "cloudpenny-tenants",
      "COGNITO_USER_POOL_ID": "ap-southeast-1_yMWin06rf",
      "CENTRAL_CURS_BUCKET": "cloudpenny-central-curs-dev"
    },
    "routes": [
      { "method": "PUT",    "path": "/api/v1/clients/me" },
      { "method": "DELETE", "path": "/api/v1/clients/me" }
    ]
  },
  {
    "name": "cloud-penny-getAwsConnection",
    "description": "Get AWS connection / external ID",
    "aws_name_override": "cloud-penny-getAwsConnection",
    "handler": "index.handler",
    "runtime": "nodejs20.x",
    "memory": 128,
    "timeout": 10,
    "environment": { "TENANTS_TABLE": "cloudpenny-tenants" },
    "routes": [
      { "method": "GET", "path": "/api/v1/aws-connection" }
    ]
  },
  {
    "name": "cloud-penny-saveAwsAccount",
    "description": "Save AWS Account ID and whitelist S3 bucket policy",
    "aws_name_override": "cloud-penny-saveAwsAccount",
    "handler": "index.handler",
    "runtime": "nodejs20.x",
    "memory": 128,
    "timeout": 15,
    "environment": { 
      "TENANTS_TABLE": "cloudpenny-tenants",
      "CENTRAL_CURS_BUCKET": "cloudpenny-central-curs-dev" 
    },
    "routes": [
      { "method": "POST", "path": "/api/v1/aws-connection" }
    ]
  },
  {
    "name": "cloud-penny-verifyAwsAccount",
    "description": "Verify CUR delivery to central S3 bucket",
    "aws_name_override": "cloud-penny-verifyAwsAccount",
    "handler": "index.handler",
    "runtime": "nodejs20.x",
    "memory": 128,
    "timeout": 15,
    "environment": { 
      "TENANTS_TABLE": "cloudpenny-tenants",
      "CENTRAL_CURS_BUCKET": "cloudpenny-central-curs-dev" 
    },
    "routes": [
      { "method": "POST", "path": "/api/v1/aws-connection/verify" }
    ]
  },
  {
    "name": "cloud-penny-postConfirmationFunction",
    "description": "Cognito post-confirmation trigger — no API route",
    "aws_name_override": "cloud-penny-postConfirmationFunction",
    "handler": "index.handler",
    "runtime": "nodejs20.x",
    "memory": 128,
    "timeout": 10,
    "environment": { "TENANTS_TABLE": "cloudpenny-tenants" },
    "routes": []
  },
  {
    "name": "cloud-penny-processCurUpdate",
    "description": "SQS Worker to process CUR updates from S3 events and enforce quota",
    "aws_name_override": "cloud-penny-processCurUpdate",
    "handler": "index.handler",
    "runtime": "nodejs20.x",
    "memory": 128,
    "timeout": 30,
    "environment": { "TENANTS_TABLE": "cloudpenny-tenants" },
    "routes": []
  }
]
